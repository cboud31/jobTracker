const pg = require('pg');
const client = new pg.Client('postgres://localhost:5432/jobTracker');

// DB adapters?

module.exports = {
  client,
  // DB Adapters,
  // DB Adapters,
  // DB Adapters,
};
